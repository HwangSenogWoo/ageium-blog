name: AGEIUM Growth OS exact ingest (manual only)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Exact RC1 ingress already completed; obtain a fresh signed source and re-attest SHA before any replay."
