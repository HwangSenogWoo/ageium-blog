name: AGEIUM Growth OS durable CDN probes (exhausted)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "StaticDelivr run 32945335402 rejected: HTML bytes transformed, exact SHA failed."
          echo "Statically run 32945464882 rejected: index.html served text/plain."
          echo "Exact RC1 remains 975c2f4495340bf62314a98def6eaf1ae0f018ef; do not replay these providers."
